#include<iostream>
using namespace std;
int factorial(int n)
{
    //Base Condition
    if(n==1)
    {
        return 1;
    }
    return n*factorial(n-1);
}
int main()
{
    int n;
    cout<<"Enter n number";
    cin>>n;
    cout<<"Factorial of a number:"<<factorial(n)<<endl;
}