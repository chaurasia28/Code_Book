#include<iostream>
using namespace std;
int getlen(char *word)
{
    int len=0;
    for(int i=0;word[i]!='\0';i++)
    {
        len++;
    }
    return len;
}
int main()
{
    char word[100];
    cin>>word;
    int len=getlen(word);
    cout<<"Length of string: "<<len<<endl;
}