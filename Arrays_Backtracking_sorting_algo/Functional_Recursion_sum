#include<iostream>
using namespace std;
int sum(int n)
{
    int ans=n;
    if(n==0)
    {
        return 0;
    }
    return ans+sum(n-1);
}
int main()
{
    int n;
    cout<<"Enter nth value";
    cin>>n;
    cout<<"Sum of N natural number:"<<sum(n);
}