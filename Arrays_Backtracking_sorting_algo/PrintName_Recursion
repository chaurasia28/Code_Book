#include<iostream>
using namespace std;
void printname(int i,int n)
{
    if(i>n){
        return;
    }
    cout<<"Srishti"<<endl;
    printname(i+1,n);
}
int main()
{
    int i,n;
    i=1;
    cout<<"Enter n value:";
    cin>>n;
    printname(i,n);
}