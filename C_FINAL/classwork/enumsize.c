#include<stdio.h>
enum day_of_week{Sunday,Monday,Tuesday,Wednesday,Thursday,Friday,Saturday};
enum day_of_week today;
//enum day_of_week today=Monday;
int main(){
   /* for(today=Monday;today<=Saturday;today++){
            printf("Size of today=%lu",sizeof(today));
    }*/
     printf("Size of today=%lu",sizeof(today));
}
